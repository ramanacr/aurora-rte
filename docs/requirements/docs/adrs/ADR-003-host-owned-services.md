# ADR-003: Keep core client-side and host-owned

**Status:** Accepted

The open-source editor has no mandatory backend and no implicit networking. Hosts own documents, identity, storage, uploads, authorization, and AI connection. Optional enterprise services use explicit documented adapters and preserve managed/self-hosted API parity.

